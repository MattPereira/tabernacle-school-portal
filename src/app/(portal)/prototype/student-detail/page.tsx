import { StudentDetailPrototype } from "@/components/student-detail-prototype";

// PROTOTYPE — #56. Three student-detail layouts at ?variant=A|B|C.
// This route deliberately uses in-memory sample data only; settle the shape
// here before building the real detail read seam and page.
export default function StudentDetailPrototypePage() {
  return <StudentDetailPrototype />;
}
